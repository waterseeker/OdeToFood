@model OdeToFood.Entities.Restaurant

<div>

    <h1>Create</h1>
    <div>

        @using (Html.BeginForm()) //html helper to make a form
        {
            <div>
                @Html.TextBoxFor(m => m.Name) <!--will write out an input tag and set properties according to info that's in the model-->
            </div>

        }

    </div>

</div>